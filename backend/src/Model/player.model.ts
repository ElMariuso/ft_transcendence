/**
 * Player Model
 * Represents a player in the system with a unique identifier and associated points.
 */
export class PlayerInQueue {
    /**
     * The unique identifier for the player.
     */
    id: number;

    /**
     * The points or score associated with the player.
     */
    points: number;
}